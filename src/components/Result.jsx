/* import { useSelector } from "react-redux";

       
    }
    return ( 
        <div>

        the result is {result(selectedOption , comSelectedOption)}
        </div>
     );
}
 
export default Result;
 */